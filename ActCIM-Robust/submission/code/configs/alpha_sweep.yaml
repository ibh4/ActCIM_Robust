extends: base
model: resnet18_cifar
data:
  batch_size: 256
  num_workers: 4
nonlinearity:
  enabled: true
  normalization_scope: per_tensor
  injection_mode: all_layers
  alpha_mode: fixed
sweep:
  alphas: [-0.8, -0.6, -0.4, -0.2, -0.1, 0.0, 0.1, 0.2, 0.4, 0.6, 0.8]
